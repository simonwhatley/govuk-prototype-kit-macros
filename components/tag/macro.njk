{% macro govukTag(params) %}
{%- if params.html.length or params.text.length %}
<strong class="phase-tag{%- if params.classes %} {{ params.classes }}{% endif %}"{% for attribute, value in params.attributes %} {{attribute}}="{{value}}"{% endfor %}>
{{ params.html | safe if params.html else params.text }}
</strong>
{% endif -%}
{% endmacro %}